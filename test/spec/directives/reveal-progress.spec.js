(function () {
  'use strict';

  describe('directives', function () {
    describe('revealProgress', function () {

      // load the directive's module
      beforeEach(module('angularRevealApp'));

      var
        scope;

      beforeEach(inject(function ($rootScope) {
        scope = $rootScope.$new();
      }));


    });
  });
})();
